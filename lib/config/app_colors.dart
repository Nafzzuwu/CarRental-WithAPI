import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Base
  static const Color background = Color(0xFF121218);
  static const Color surface = Color(0xFF1C1C24);
  static const Color card = Color(0xFF232330);

  // Primary & Accent
  static const Color primary = Color(0xFF5B8FB9);
  static const Color secondary = Color(0xFFD4A574);

  // Semantic
  static const Color success = Color(0xFF66BB6A);
  static const Color error = Color(0xFFEF5350);
  static const Color warning = Color(0xFFFFA726);

  // Text
  static const Color textPrimary = Color(0xFFF5F5F5);
  static const Color textSecondary = Color(0xFF9E9E9E);
  static const Color textHint = Color(0xFF616161);

  // Border
  static const Color border = Color(0xFF2A2A36);
}
